

<div class="container">
	<div class="row"></div>
		<div class="col-md-4"></div>
			<div class="col-md-4">
			
				<div class="page-title">Products</div>
					<%=  @products.each do |product|%>
					<div class="items"><%= link_to product.product_name, product_path(product.id), class: "btn btn-success btn-lg btn-width"  %></div>
				<% end %>

			</div>
			<div class="col-md-4"></div>
	
	</div>
</div>